enum Shape {
    Circle(f64),
    Rect(f64, f64),
    Point,
}

fn area(shape: Shape) -> f64 {
    match shape {
        Shape::Circle(r) => 3.14 * r * r,
        Shape::Rect(w) => w * w,
        Shape::Point => 0.0,
    }
}

fn main() {
    println!("{}", area(Shape::Point));
}
